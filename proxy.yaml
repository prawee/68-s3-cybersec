services:
  caddy:
    image: caddy:latest
    container_name: 68-s3-proxy
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./Caddyfile:/etc/caddy/Caddyfile
      - caddy_data:/data
      - caddy_config:/config
    depends_on:
      - s3app

volumes:
  caddy_data:
  caddy_config:
